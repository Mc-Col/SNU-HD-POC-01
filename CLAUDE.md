# CLAUDE.md — 공통 컨텍스트

이 파일은 Claude Code 가 자동으로 읽는다. 작업 전 반드시 이 규칙을 따른다.

## 프로젝트

**VLM 기반 연속공정 디지털트윈 Asset master agent 1단계 개발 PoC**

비정형 설비 문서(스캔 이미지 · PDF · 엑셀)에서 컨트롤밸브 기준정보 **28필드**를
추출·검증해 마스터 스키마 엑셀로 내보낸다. 사람이 최종 확정한다.

대상 1,021건 중 `tif`(스캔) 71.9% · `pdf` 16.6% · `xlsx` 11.6%. **VLM 이 주 경로다.**
정비·개조 보고서 112건도 대상이다 — 사양 칸이 비면 `N/A` + 사유. 억지로 채우지 않는다.

- 발표: 2026-08-27
- 산출물 기준: `docs/ARCHITECTURE.md`
- 설계 판단 근거: `docs/insight_memory.md`
- 남은 작업·대기 결정: `docs/TODO.md`
- 평가 실행 이력: `docs/eval_history.md`

## 데이터 흐름

```
파일 ─▶ ① Triage ─▶ ② Router ─▶ ③ Parser(Text|VLM) ─▶ ④ Normalize
     ─▶ ⑤ Validate(형식→도메인→평가Agent) ─▶ ⑥ State ─▶ ⑦ HITL ─▶ 엑셀
```

계약은 `src/contracts.py` 세 개뿐이다: `TriageResult` → `RawExtraction` → `FieldRecord`.

전처리 도구는 `src/preprocess.py` 에 모여 있다 — 파일명 판정 · 페이지 렌더 ·
텍스트 레이어 탐침 · 최신성 선택. 각 모듈은 이것을 **조립해서 판정한다.**
다시 만들지 않는다.

## 개발 철학 — 6개

1. **계약 밖을 만지지 않는다.**
   자기 모듈 폴더만 수정한다. `src/contracts.py`, `schema/*.yaml`, `src/pipeline.py`,
   `src/hooks.py`, `src/preprocess.py` 는 소유자만 수정한다. 변경이 필요하면 요청한다.
2. **규칙은 코드가 아니라 `schema/*.yaml` 에.**
   단위·표기·도메인 규칙을 파이썬에 하드코딩하지 않는다. 도메인 전문가가 검토할 수
   있어야 하고, PoC 가 중단되어도 남는 산출물이다.
3. **모든 모듈은 fixture 로 자기 검증이 가능해야 한다.**
   `fixtures/<모듈>/` 에 입력과 기대 출력을 둔다. 남의 모듈을 기다리지 않는다.
4. **근거 없는 값을 만들지 않는다.**
   모르면 `state=NA` + `note`. 추정값이 마스터DB 에 들어가면 이 과제가 해결하려는
   문제를 재생산한다.
5. **실패를 삼키지 않는다.**
   예외를 조용히 넘기지 않고 `on_error` 로 기록한다. 처리 실패율도 측정 대상이다.
6. **같은 입력 → 같은 출력.**
   난수·시각에 의존하지 않는다. 개선을 측정할 수 없게 된다.

## 절대 하지 말 것

- **`raw_file/` 을 Git 에 올리지 않는다.** 회사 문서 1,059건이다. `.gitignore` 에 있다.
  `.gitignore` 에서 **줄 끝 주석은 동작하지 않는다** — 주석은 따로 줄을 쓴다.
- **`.env`(API 키)를 올리지 않는다.**
- **재시도 프롬프트에 "값이 틀렸다"고 쓰지 않는다.** 환각을 유도한다.
  올바른 문구: *"이 영역에 문자 그대로 무엇이 적혀 있는지 보고하라. 같은 값이면 같다고 답하라"*
- **제약 위반에 재시도를 걸지 않는다.** 재시도는 추출 실패(못 읽음)에만.
- **태그 단독성으로 사양표 페이지를 고르지 않는다.** 최신성으로 고른다.
  `10FV011` 은 단독 태그 쪽이 1986년 폐기본이고, 그걸 고르면 MVP 2필드가 틀린다.
- **텍스트 레이어가 있다고 VLM 을 건너뛰지 않는다.** 텍스트는 글자를 보증할 뿐이고
  어느 값이 어느 필드인지는 VLM 이 정한다.
- **이미지 크기 조정(업스케일·축소·크롭)을 묻지 않고 켜지 않는다.**
  스위치는 이미 있고 **기본은 전부 꺼져 있다** — `D2S_LONG_EDGE`(목표 장변) ·
  `D2S_UPSCALE`(배율) · `D2S_CROP_ZOOM` · `D2S_PREP`. 새로 만들지 말고 이것을 쓴다.
  **켜기 전에 `src/parsers/vlm/CLAUDE.md` 의 「이미지 크기 조정」 절을 읽는다.**
  **포화점은 배율이 아니라 절대 장변 약 2500~2600px 이다** — 배율 모형은 두 번 반증됐다.
  ⚠️ `D2S_LONG_EDGE` 를 켜면 `D2S_UPSCALE` 이 **아예 읽히지 않는다.** 팀 확정 구성의
  2배 확대가 무효화되므로, 2배로 돌고 있다고 믿는 실행이 실제로는 1.47배로 돈다.
  사용자에게 ① 확정 구성을 덮어도 되는지 ② 기본값 승격 여부 ③ 토큰 비용을 확인하고 시작한다.
- **홀드아웃 문서를 열지 않는다.** `docs/TODO.md` 의 🔒 절이 목록이다.
  **킷에 홀드아웃 표시 열이 없어서** `--only` 로 포맷을 지정하면 딸려 들어온다 —
  2026-08-26 에 tif 3건이 그렇게 개봉됐다. 실행 전에 목록을 대조한다.

## 결정된 규칙

`docs/ARCHITECTURE.md` 에 전부 있다. 특히 §4 (태그 · 페이지 선택 · 범위 · 함정)를
읽지 않고 시작하면 이미 내린 결정을 다시 발명한다.

## 내 역할 찾기

`docs/roles/` 에서 자기 이름 파일을 읽는다. 자기 모듈 폴더의 `CLAUDE.md` 도 함께 읽는다.
